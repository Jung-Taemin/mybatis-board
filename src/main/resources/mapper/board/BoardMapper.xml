<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mybatisboard.domain.board.mapper.BoardMapper">

    <select id="findAll" resultType="com.example.mybatisboard.domain.board.dto.BoardResponse">
        SELECT id, title, content, writer, hits, created_at AS createdAt, updated_at as updatedAt
        FROM boards
        ORDER BY id DESC
    </select>

    <select id="findById" resultType="com.example.mybatisboard.domain.board.dto.BoardResponse">
        SELECT id, title, content, writer, hits, created_at AS createdAt, updated_at AS updatedAt
        FROM boards
        WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO boards(title, content, writer)
        VALUES (#{req.title}, #{req.content}, #{req.writer})
    </insert>

    <update id="update">
        UPDATE boards
        SET title = #{req.title}, content = #{req.content}
        WHERE id = #{id}
    </update>

    <update id="increaseHits">
        UPDATE boards
        SET hits = hits + 1
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM boards
        WHERE id = #{id}
    </delete>

</mapper>